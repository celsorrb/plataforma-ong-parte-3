/**
 * js/app.js
 * Ponto de entrada da aplicação.
 */
import { initSPA } from './spa.js';

document.addEventListener('DOMContentLoaded', () => {
    // Inicia o sistema de navegação do SPA
    initSPA(); 
});